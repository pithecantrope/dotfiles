# To install packages, run:
# sed 's/ *#.*//; /^$/d' .packages | sudo pacman -S --needed -
# Software with a leading '#' must be installed manually.
   # A)
   # B)
bat                    # Modern `cat`
brightnessctl          # Brightness control
btop                   # Resources monitor
   # C)
#chatterino2-bin       # Chat client 'yay -S chatterino2-bin'
   # D)
dmenu                  # Dynamic menu
dunst                  # Notification daemon
#dwm                   # Window manager `git clone https://git.suckless.org/dwm`
   # E)
easyeffects            # Audio enhancer
eza                    # Modern `ls`
   # F)
fastfetch              # System information
fd                     # Modern `find`
firefox                # Web browser
firefox-tridactyl      # Vim-like interface
fzf                    # Fuzzy finder
   # G)
git                    # Version control
git-delta              # `git` pager
glow                   # CLI markdown
gnome-themes-extra     # Dark mode
   # H)
helix                  # Text editor
   # I)
   # J)
   # K)
keyd                   # Key remapper
   # L)
libreoffice-still-ru   # Office suite
ly                     # Display manager
   # M)
maim                   # Screenshot maker
mpv                    # Media player
   # N)
   # O)
obs-studio             # Live streaming
openssh                # Remote connection
ouch                   # Compression decompression
   # P)
pacman-contrib         # Cache cleaner
#pinta                 # Paint program 'yay -S pinta'
python-qrcode          # QR code
   # Q)
   # R)
reflector              # Mirror manager
ripgrep                # Modern `grep`
   # S)
#st                    # Suckless terminal `git clone https://git.suckless.org/st`
   # T)
tlp                    # Battery life
tokei                  # Code counter
ttf-cascadia-mono-nerd # Terminal font
   # U)
unclutter              # Cursor hider
   # V)
vivid                  # `LS_COLORS` generator
   # W)
wireless-regdb         # Regulatory database
   # X)
xclip                  # X11 Clipboard
xorg                   # Window system
   # Y)
#yay                   # AUR helper `sudo pacman -S --needed git base-devel && git clone https://aur.archlinux.org/yay.git && cd yay && makepkg -si`
yt-dlp                 # Video downloader
   # Z)
